export const calculator = {
  add(a, b) {
    const sum = a + b;

    return sum;
  },

  sub(a, b) {
    const result = a - b;

    return result;
  },

  divide(a, b) {
    const result = a / b;

    return result;
  },

  multiply(a, b) {
    const result = a * b;

    return result;
  },
};
